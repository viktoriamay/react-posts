import { RegisterForm } from "./RegisterForm";

export const Authorization = (
  activeModal,
  setActiveModal,
  handleCloseModal,
) => {
  return (
    <div>
      <RegisterForm />
    </div>
  );
};
